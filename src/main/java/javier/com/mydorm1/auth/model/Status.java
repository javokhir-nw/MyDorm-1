package javier.com.mydorm1.auth.model;

public enum Status {
    ACTIVE,
    INACTIVE,
    DELETED,

    PRESENT,
    ABSENT,

    SENT_ROOM,
    SENT_FNAME,
    SENT_LNAME,
    SENT_MNAME,
    SENT_PNUMBER
}
